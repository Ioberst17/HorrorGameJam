using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Used to denote where the player's projectile fire point is
/// </summary>
public class PlayerFirePointAnimator : PlayerBaseAnimator
{
    // Start is called before the first frame update
    override public void Start()
    {
        base.Start();
    }


}
